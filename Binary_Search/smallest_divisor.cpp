//Find the Smallest Divisor Given a Threshold

#include<bits/stdc++.h>
using namespace std;

int smallest_divisor(vector<int> )

int main(){
    vector<int> num = {1,2,5,9};
    int threshold = 6;
    int result = smallest_divisor(num, threshold);
    cout << result << endl;
    return 0;
}